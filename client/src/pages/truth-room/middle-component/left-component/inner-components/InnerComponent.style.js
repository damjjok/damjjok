import styled from "styled-components";
// 왼쪽 컴포넌트 안에 들어가는 참여 인원, 제보, 출석부에 적용할 styled div

export const Wrapper = styled.div`
    width: 12vw;
    height: 65vh;
    margin: 0 4vw;
    border-radius: 20px;
    background-color: #515151;
    box-shadow: 8px 8px rgba(0, 0, 0, 0.1);
`;
